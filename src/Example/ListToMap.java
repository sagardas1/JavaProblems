package Example;

import java.util.*;
import java.util.stream.Collectors;

public class ListToMap
{
    public static void main(String[] args) {
        Random random
                =new Random();
//        random.ints(1,10).limit(10).distinct()
//                .sorted(Collections.reverseOrder(Comparator.comparing(i->i))).collect(Collectors.toList());

//        Employee employee=new Employee("sagar",20,2000);
//        Employee employee1=new Employee("ram11",22,1000);
//        Employee employee10=new Employee("ram11",22,1000);
//        Employee employee2=new Employee("sid",10,2333);
//
//
//
       List<Employee> list=new ArrayList<>();

     //  list.stream().filter()
     //  list.stream().sorted(Collections.reverseOrder(Comparator.comparing(p->p.getAge()))).collect(Collectors.toList());
////        Map<String, Long> counters = list.stream()
////                .collect(Collectors.groupingBy(p -> p.getName(),
////                        Collectors.counting()));
//        list.add(employee);
//        list.add(employee1);
//        list.add(employee2);
//
//
//        list.stream().sorted(Comparator.comparing(Employee::getName).thenComparing(Employee::getSalary)).collect(Collectors.toList());
//       // list.add(employee10);
//
////        Map<String ,Employee> map=list.stream().collect(Collectors.toMap(p->p.getName(), p->p));
////
////        System.out.println(map);

    }
}
